# ARCHITECTURE.md — 项目架构文档

> 给"非技术读者 + 技术读者"双向阅读的架构说明

## 一、整体架构

游戏采用**经典的模块化分层架构**，核心思想是「职责分离」：

```
┌─────────────────────────────────────────────────────────────┐
│                         main.py                              │
│   入口：pygame 初始化、主循环、事件分发、坐标缩放输出        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      game/game.py                            │
│   Game 主控：状态机、输入处理、生命周期管理（745 行）        │
└─────────────────────────────────────────────────────────────┘
        │                       │                       │
        ▼                       ▼                       ▼
┌──────────────┐      ┌────────────────┐      ┌──────────────┐
│ game/wave.py │      │ game/renderer.py│      │game/config.py│
│ 波次生成     │      │ 全部绘图逻辑   │      │ 集中配置     │
│ + 预告文案   │      │ （927 行）     │      │ (dataclass)  │
└──────────────┘      └────────────────┘      └──────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│                       实体层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ game/enemy.py│  │ game/tower.py│  │game/bullet.py│        │
│  │ 敌人 + Boss  │  │ 防御塔       │  │ 子弹         │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│                       支撑层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │game/assets.py│  │game/effects.py│ │ game/utils.py│        │
│  │ 资源管理器   │  │ 对象池       │  │ 工具函数     │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│                    data/levels.json                           │
│         关卡路径外置数据（带内置回退机制）                    │
└──────────────────────────────────────────────────────────────┘
```

**核心设计原则**：

1. **逻辑与渲染分离**：`Game` 不直接画图，所有绘图委托给 `Renderer`
2. **配置外置**：路径数据从 JSON 加载，改关卡不动代码
3. **对象池**：粒子、激光用环形索引复用，避免运行时 GC 卡顿
4. **类型安全**：核心数据用 `@dataclass`，IDE 友好

---

## 二、状态机（Game.state）

`Game.state` 是一个字符串，控制游戏当前所处的画面：

```
                  ┌────────┐
                  │  menu  │ ← 主菜单（开始/说明/关于 + 难度选择）
                  └───┬────┘
                      │ 点击"开始游戏"
                      ▼
              ┌──────────────┐
              │ level_intro  │ ← 关卡介绍（3秒倒计时或点击跳过）
              └──────┬───────┘
                     │ 计时结束/点击
                     ▼
                ┌─────────┐
        ┌──────│ playing │◀──┐
        │      └────┬────┘   │
        │           │        │
        │   生命=0  │        │ 通关
        │           ▼        │
        │    ┌──────────┐    │
        │    │game_over │    │
        │    └──────────┘    │
        │           │        │
        │      返回菜单       │
        │           ▼        │
        │      ┌────────┐    │
        │      │  menu  │    │
        │      └────────┘    │
        │                    │
        └────── 10关通关 ────┘
                  │
                  ▼
            ┌─────────┐
            │ victory │ ← 通关画面（5秒后回菜单）
            └─────────┘
```

中间还穿插：
- `guide`：游戏说明页
- `about`：关于本作品页

---

## 三、数据流（每帧循环）

```
每帧 (60 FPS)
  │
  ├─ 1. 事件分发 (main.py)
  │     - QUIT / VIDEORESIZE / MOUSEBUTTONDOWN / MOUSEMOTION / KEYDOWN / MOUSEWHEEL
  │
  ├─ 2. Game.update(dt) (game.py)
  │     - 根据 state 走不同更新路径
  │     - playing 状态：波次生成 → 敌人更新 → 死亡处理 → 塔更新 → 子弹更新 → 特效更新
  │
  ├─ 3. Game.draw(renderer) (game.py)
  │     - 根据 state 调用 renderer 对应方法
  │     - 渲染顺序：背景 → 路径 → 塔 → 激光 → 敌人 → 子弹 → 粒子 → UI
  │
  └─ 4. 输出到屏幕 (main.py)
        - canvas → 缩放 → window_screen.blit → pygame.display.flip()
```

---

## 四、关键模块详解

### 1. `config.py` — 集中配置

使用 `@dataclass` 定义核心数据结构：

```python
@dataclass
class TowerStats:
    name: str
    cost: int
    range: int
    damage: int
    fire_rate: float
    color: Tuple[int, int, int]
    rapid_fire: bool = False
    crit_chance: float = 0.0
    special: Optional[str] = None
    # ...
```

加新塔只需：
1. 在 `_default_tower_types()` 加一项
2. 在 `_default_upgrade_costs()` 加升级费用
3. 加资源文件 `assets/towers/<name>.png`

### 2. `assets.py` — 资源管理器

```
AudioManager       音效/背景音乐（带 BGM 缺失自动回退）
ImageManager       图片加载（带精灵缺失自动回退到几何图形）
FontManager        字体加载（带缓存）
```

### 3. `effects.py` — 对象池

```
ParticlePool(600)  粒子环形索引（生成、复用）
LaserManager(30)   激光环形索引（生成、复用）
```

**为什么需要对象池？** pygame 不像 Unity 有内置 GC，频繁创建/销毁 Surface 会卡顿。用对象池预分配，按环形索引复用，零运行时分配。

### 4. `tower.py` — 目标选择策略

```python
def _find_target(self, enemies):
    """选择射程内沿路径最远的敌人。"""
    target = None
    best = -1
    for e in enemies:
        # ... 射程/飞行/隐身 过滤
        if e.progress > best:
            best = e.progress
            target = e
    return target
```

塔选目标是「沿路径进度最大」的敌人（即最接近终点的），这是塔防游戏的主流策略。

### 5. `enemy.py` — Boss AI

```
每帧:
  1. 毒伤 / 减速 / 冻结计时器递减
  2. 计算速度倍率（冰冻=0, 冰-慢<毒-慢<原速）
  3. 坦克护盾再生（5秒CD）
  4. 快速敌人加速光环（范围内友军 +50% 速度）
  5. Boss 减速塔（每7秒对最近2塔减速4秒）
  6. Boss 半血隐身 + 召唤增援（一次性）
  7. 沿路径移动 / 飞行敌人直线移动
```

### 6. `renderer.py` — 路径预渲染

`_draw_map()` 用关卡级缓存避免每帧重绘路径：

```python
# 第一帧：渲染路径到离屏 Surface（多层：发光 → 边框 → 主体 → 高光 → 纹理 → 碎石）
# 后续帧：直接 blit 缓存的 Surface
if not hasattr(self, '_path_cache') or self._path_cache['_level'] != current_level:
    self._path_cache = self._render_path_surface(path, current_level)
```

---

## 五、扩展指南

### 加新关卡
编辑 `data/levels.json`，加新路径数组即可（路径 X 坐标范围 0-720，Y 0-600）。

### 加新敌人
1. `config.py` 的 `_default_enemy_base()` 加 EnemyStats
2. `wave.py` 的生成逻辑里引用
3. `enemy.py` 加新类型专属行为（可选）
4. `assets/enemies/` 加 3 帧动画

### 加新塔
1. `config.py` 的 `_default_tower_types()` 加 TowerStats
2. `_default_upgrade_costs()` 加升级费用
3. `tower.py` 的 `_fire()` 加新分支
4. `bullet.py` 加新类型子弹的绘制（如果新塔有特殊子弹）
5. `assets/towers/` 加精灵图

### 加新游戏状态
1. `game.py` 的 `Game.__init__` 加新 state 变量
2. `update()` 加 state 分支
3. `draw()` 加 state 分支
4. `renderer.py` 加新 `draw_xxx()` 方法
5. `_handle_xxx_click()` 加点击处理

---

## 六、性能与内存

| 项 | 数值 |
|---|---|
| 目标帧率 | 60 FPS |
| 屏幕分辨率 | 960 × 600 |
| 粒子池容量 | 600 |
| 激光池容量 | 30 |
| 音乐通道 | 16 |
| 最大同时敌人 | 受 spawn_queue 控制（一般 20-50） |

性能瓶颈一般出现在：
- 大量粒子同时活跃（>300）→ 减少 `ParticlePool.spawn()` 调用
- 大量子弹同时存在 → 检查 `_apply_area_slow` 这种 O(n²) 操作

---

## 七、调试与排错

### 调试快捷键（游戏中按键序列）
- `next` + 回车 → 跳关
- `money` + 回车 → +500 金币

### 常见问题
- **音效/图片加载失败**：会 `print` 警告，游戏会用几何图形回退，不影响运行
- **关卡路径异常**：JSON 解析失败时自动回退到内置默认路径
- **性能掉帧**：检查是否有粒子/激光爆量

---

**这份文档与代码一起维护，是机构内部教学的参考资料。**